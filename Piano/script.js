function playSound(note){
    console.log(`play sound: ${note}`);
    //play corect mp3 file
}

const playSound2 = () => {
    console.log(`play sound: ${note}`);
}


const buttonHTMLCollection = document.getElementsByTagName('button');
const buttons= Array.from("buttonHTMLCollection");
const buttons2 = [...buttonHTMLCollection];



document.getElementsByTagName('button').addEventListener("click", calculate());
